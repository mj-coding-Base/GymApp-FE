name: Build & Deploy to VPS

on:
  push:
    branches:
      - deploytest

jobs:
  build:
    runs-on: ubuntu-latest

    outputs:
      node-version: ${{ steps.node.outputs.version }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      id: node
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install

    - name: Build the app
      run: npm run build

    - name: Archive production build
      run: |
        mkdir deploy
        cp -r .next package.json package-lock.json public deploy/
        cd deploy
        tar -czf ../app.tar.gz .

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-build
        path: app.tar.gz

  upload:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        ssh-keyscan -H ec2-54-89-62-199.compute-1.amazonaws.com >> ~/.ssh/known_hosts

    - name: Download build artifact
      uses: actions/download-artifact@v4
      with:
        name: app-build
        path: .

    - name: Upload build to VPS
      run: |
        scp -i ~/.ssh/id_ed25519 app.tar.gz ubuntu@ec2-54-89-62-199.compute-1.amazonaws.com:/home/ubuntu/apps/frontend/

  deploy:
    needs: upload
    runs-on: ubuntu-latest

    steps:
    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        ssh-keyscan -H ec2-54-89-62-199.compute-1.amazonaws.com >> ~/.ssh/known_hosts

    - name: Deploy on VPS
      run: |
        ssh -i ~/.ssh/id_ed25519 ubuntu@ec2-54-89-62-199.compute-1.amazonaws.com << 'EOF'
          cd /home/ubuntu/apps/frontend
          tar -xzf app.tar.gz
          rm app.tar.gz
          npm install --production

          if pm2 list | grep -q "frontend"; then
            echo "Restarting existing PM2 process: frontend"
            pm2 restart frontend
          else
            echo "Starting new PM2 process: frontend"
            pm2 start npm --name frontend -- start
          fi

          pm2 save
        EOF
